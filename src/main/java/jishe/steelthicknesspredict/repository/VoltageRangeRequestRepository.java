package jishe.steelthicknesspredict.repository;

import jishe.steelthicknesspredict.pojo.VoltageRangeRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VoltageRangeRequestRepository extends JpaRepository<VoltageRangeRequest, Long> {
}
